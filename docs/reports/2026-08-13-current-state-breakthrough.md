# Current-season state breakthrough — 2026-08-13

## 배경

recent-form residual correction까지의 Public score는 903.88440이었습니다. 기존 피처는
통산 이력과 최근 경기 요약을 포함했지만, 평가 행에 이미 주어진 누적값과 공식 과거
시즌 endpoint의 차이를 이용해 당해 시즌 상태를 명시적으로 복원하지는 못했습니다.

## 가설

투수와 타자의 현재 시즌 표본 수·성공 수·비율을 각 행에서 독립적으로 복원하면, 장기
career prior와 다른 상태 변화를 잡을 수 있습니다. 모든 endpoint는 공식 train에서
미리 계산해 고정하고, 추론 시에는 현재 행만 lookup에 사용했습니다.

## 결과

| 항목 | 값 |
| --- | ---: |
| 변경 전 Public | 903.88440 |
| 변경 후 Public | **1003.41585** |
| 개선 | **+99.53145** |

singleton, batch, shuffle 조건의 행 독립성 감사를 통과했습니다. 이 실험은 프로젝트에서
가장 큰 개인 단일 개선이었고, 이후 팀 모델에서도 current-season state를 별도 branch나
gate로 다루는 설계의 근거가 됐습니다.

## 배운 점

- 새 알고리즘보다 시간 기준이 정확한 상태 표현이 더 큰 효과를 만들 수 있습니다.
- 평가 행끼리 누적하지 않아도 현재 행과 frozen train lookup만으로 상태를 복원할 수 있습니다.
- 큰 개선일수록 추론 독립성 감사를 모델 성능과 같은 수준의 출시 조건으로 봐야 합니다.

